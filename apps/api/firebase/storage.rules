rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Files are delivered and uploaded only through the Fastify API.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
